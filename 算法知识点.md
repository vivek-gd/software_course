## 一、基础知识

### 一、算法基本概念

#### 1. 问题与算法的定义

- **问题**：包含若干参数的一般性提问，对参数的一组赋值构成一个具体实例。
  例如，调度问题中，任务集 (S) 和加工时间 (t_j) 是参数，给定具体的任务集和加工时间即构成一个实例。
- **算法**：有限条指令的集合，对问题的任意实例能在有限步内停机并输出正确解。
  例如，调度问题的贪心算法（按加工时间升序排列任务）能有效求解总等待时间最小化问题。

#### 2. 时间复杂度

- 基本概念

  ：通过计数算法的基本运算次数衡量效率，分为最坏时间复杂度 (W(n))（输入规模为 (n) 时的最长时间）和平均时间复杂度 (A(n))（输入概率分布下的平均时间）。

  - 例：顺序搜索算法的最坏时间 (W(n) = n)，平均时间 (A(n) = \frac{p(n+1)}{2} + (1-p)n)（(p) 为目标元素存在的概率）。

- 实例分析

  ：

  - 插入排序、冒泡排序：最坏和平均时间复杂度均为 (O(n^2))。
  - 快速排序：最坏 (O(n^2))，平均 (O(n \log n))。
  - 堆排序、归并排序：最坏和平均均为 (O(n \log n))。

#### 3. 典型问题举例

- **调度问题**：通过贪心策略（短任务优先）证明最优解，总等待时间公式为 (\min \sum_{k=1}^n (n-k+1)t_{i_k})。
- **货郎问题**：求经过所有城市的最短闭合路径，目前无有效算法，属于难解问题。
- **0-1背包问题**：在重量限制下最大化价值，通过动态规划等方法求解。

### 二、算法的伪码表示

#### 1. 基本语句

- **赋值语句**：`j ← 1`（将值赋给变量）。
- **分支语句**：`if … then … [else …]`（条件判断）。
- **循环语句**：`while`、`for`、`repeat until`（重复执行）。
- **输出语句**：`return j`（返回结果）。

#### 2. 实例：改进的顺序检索

```伪码
算法1.2 Search(L, x)
输入：数组 L[1..n]（非降序），数 x
输出：x 的位置 j（不存在则为 0）
1. j ← 1
2. while j ≤ n 且 x > L[j] do j ← j + 1
3. if x < L[j] 或 j > n then j ← 0
4. return j
```

- 功能：在有序数组中查找 (x) 的首次出现位置，最坏时间 (O(n))。

### 三、数学基础知识

#### 1. 函数的渐近界

##### （1）五大符号定义

- **大O（上界）**：
  $$
  存在正数 
  
  (c, n_0)
  
  ，使得 
  
  (f(n) \leq cg(n))
  
   对所有 
  
  (n \geq n_0)
  
   成立，如 
  
  (n^2 + n = O(n^2))
  
  。
  $$
  
  
- **大Ω（下界）**：
  $$
  存在正数
  
  (c, n_0)
  
  ，使得 
  
  (f(n) \geq cg(n))
  
   对所有
  
  (n \geq n_0)
  
  成立，如
  
  (n^2 + n = \Omega(n^2))
  
  。
  $$
  
  
- **小o（严格上界）**：
  $$
  对任意正数 
  
  (c)
  
  ，存在
  
  (n_0)
  
  ，使得 
  
  (f(n) < cg(n))
  
   对所有
  
  (n \geq n_0)
  
  成立，如
  
  (n^2 = o(n^3))
  $$
  

- **小ω（严格下界）**：
  $$
  对任意正数 (c)，存在 (n_0)，使得 (f(n) > cg(n)) 对所有 (n \geq n_0) 成立，如 (n^2 = \omega(n))。
  $$
  

- **Θ（紧确界）**：同时满足 (f(n) = O(g(n))) 和 (f(n) = \Omega(g(n)))，如 (n^2 + n = \Theta(n^2))。

##### （2）重要定理

- 定理1.1（极限判定）

  ：

  - $$
    若 (\lim_{n\to\infty} \frac{f(n)}{g(n)} = c > 0)，则 (f(n) = \Theta(g(n)))。
    $$

    
  - $$
    若极限为 0 或 (+\infty)，则分别对应 (f(n) = o(g(n))) 或 (f(n) = \omega(g(n)))。
    $$

    

- **定理1.2（传递性）**：
  $$
  若 (f = O(g)) 且 (g = O(h))，则 (f = O(h))（同理适用于 (\Omega) 和 (\Theta)）。
  $$
  

##### （3）函数阶的比较

- 指数级 > 多项式级 > 对数多项式级

  ：

  - $$
    指数级：(2^n, n!)；多项式级：(n^2, n \log n)；对数级：(\log n, \log^2 n)。
    $$
  
    
  - $$
    例：(n! = o(n^n))，(\log(n!) = \Theta(n \log n))（利用 Stirling 公式）。
    $$
  
    

#### 2. 求和方法

##### （1）直接求和公式

- **等差级数**：
  $$
  (\sum_{k=1}^n a_k = \frac{n(a_1 + a_n)}{2})。
  $$
  
- **等比级数**：
  $$
  (\sum_{k=0}^n ar^k = \frac{a(1-r^{n+1})}{1-r})（(r \neq 1)）。
  $$
  
- **调和级数**：
  $$
  (\sum_{k=1}^n \frac{1}{k} = \ln n + O(1))。
  $$
  

##### （2）放缩法估计上界

- $$
  若 (\frac{a_{k+1}}{a_k} \leq r < 1)，则 (\sum_{k=0}^n a_k \leq \frac{a_0}{1-r})。
  $$
  
  
  - $$
    例：(\sum_{k=1}^n \frac{k}{3^k} \leq 1)（利用 (r = 2/3)）。
    $$
  
    

##### （3）积分近似

- $$
  - (\sum_{k=1}^n \frac{1}{k}) 的渐近界：(\ln(n+1) \leq \sum_{k=1}^n \frac{1}{k} \leq \ln n + 1)。
  
  #### 3. 递推方程求解
  $$

  

##### （1）迭代法

- **直接迭代**：展开递推式直至初始条件，如
  $$
  插入排序的最坏时间递推式 (W(n) = W(n-1) + n-1)，解得 (W(n) = \frac{n(n-1)}{2})。
  $$
  
- **换元迭代**：通过变量替换简化递推式，如
  $$
  归并排序 (W(n) = 2W(n/2) + n-1)，令 (n = 2^k)，解得 (W(n) = n \log n - n + 1)。
  $$
  

##### （2）递归树

- 构建方法

  ：将递推式展开为树状结构，每层表示子问题和开销，总开销为各层之和。

  - 例：
    $$
    归并排序递归树每层开销为 (n-1, n-2, \dots)，总开销 (O(n \log n))。
    $$
    

##### （3）主定理

$$
（针对 (T(n) = aT(n/b) + f(n))）
$$



- 情况1

  ：
  $$
  若 (f(n) = O(n^{\log_b a - \varepsilon}))（(\varepsilon > 0)），则 (T(n) = \Theta(n^{\log_b a}))。
  $$
  

  - 例：
    $$
    (T(n) = 9T(n/3) + n)，(\log_3 9 = 2)，(f(n) = n = O(n^{2-1}))，故 (T(n) = \Theta(n^2))。
    $$
    

- 情况2

  ：
  $$
  若 (f(n) = \Theta(n^{\log_b a}))，则 (T(n) = \Theta(n^{\log_b a} \log n))。
  $$
  

  - 例：
    $$
    (T(n) = T(2n/3) + 1)，(\log_{3/2} 1 = 0)，(f(n) = 1 = \Theta(n^0))，故 (T(n) = \Theta(\log n))。
    $$
    

- 情况3

  ：
  $$
  若 (f(n) = \Omega(n^{\log_b a + \varepsilon})) 且满足正则条件，则 (T(n) = \Theta(f(n)))。
  $$
  
  
  - 例：
    $$
    (T(n) = 3T(n/4) + n \log n)，(\log_4 3 \approx 0.793)，(n \log n = \Omega(n^{0.793+0.2}))，故 (T(n) = \Theta(n \log n))。
    $$
    

### 四、总结

文档系统介绍了算法分析的基础知识，从问题与算法的定义到时间复杂度分析，再到数学工具（渐近界、求和、递推方程）的应用。这些内容是算法设计与优化的核心，尤其主定理在递归算法分析中至关重要，而渐近界的符号和性质则是复杂度分析的基础语言。通过典型实例（如排序、调度问题）的讲解，结合伪码和数学推导，形成了完整的算法分析框架

## 二、分治策略

### 分治策略详解

#### 一、分治策略基本思想

分治策略（Divide and Conquer）通过将复杂问题分解为若干独立子问题，递归求解子问题，再合并结果得到原问题的解。核心步骤包括：

1. **分解（Divide）**：将问题划分为规模更小的同类子问题（通常均匀划分）。
2. **求解（Conquer）**：递归求解每个子问题（规模足够小时直接求解）。
3. **合并（Merge）**：将子问题的解合并为原问题的解。

##### 典型例子

- **二分检索（Binary Search）**

  - **算法**：在有序数组中通过不断比较中间元素缩小搜索范围，
    $$
    最坏时间复杂度 (W(n) = \lfloor \log n \rfloor + 1)（递推方程：(W(n) = W(\lfloor n/2 \rfloor) + 1)）。
    $$
    

  - **伪码**：通过调整左右边界 (l, r) 和中间位置 (m)，逐步排除不可能包含目标值的区间。

- **二分归并排序（Merge Sort）**

  - **步骤**：将数组分成两半，递归排序后合并；合并过程通过双指针线性时间完成。

  - **时间复杂度**：
    $$
    最坏情况 (W(n) = 2W(n/2) + n-1 = \Theta(n \log n))，通过主定理或递归树求解。
    $$
    

#### 二、分治算法分析技术

分治算法的效率分析依赖递推方程，主要有两类形式：

1. **子问题规模为 (n-i)**
   $$
   （如Hanoi塔：(W(n) = 2W(n-1) + 1)）。
   $$
   

2. **子问题规模为 (n/b)**
   $$
   （如归并排序：(W(n) = aW(n/b) + f(n))）。
   $$
   

##### 主定理（Master Theorem）

$$
针对 (T(n) = aT(n/b) + f(n))（(a \geq 1, b > 1)）
$$

：

- **情况1**：
  $$
  若 (f(n) = O(n^{\log_b a - \varepsilon}))（(\varepsilon > 0)），则 (T(n) = \Theta(n^{\log_b a}))（如Strassen矩阵乘法）。
  $$
  

- **情况2**：
  $$
  若 (f(n) = \Theta(n^{\log_b a}))，则 (T(n) = \Theta(n^{\log_b a} \log n))（如归并排序）。
  $$
  

- **情况3**：
  $$
  若 (f(n) = \Omega(n^{\log_b a + \varepsilon})) 且满足正则条件，则 (T(n) = \Theta(f(n)))（如平面点对问题）。
  $$
  

#### 三、改进分治算法的途径

##### 1. **减少子问题个数（代数变换）**

通过数学变换减少独立子问题数量，降低时间复杂度。

- **位乘问题**：传统分治需4次子问题乘法，通过 (AD + BC = (A+B)(D+C) - AC - BD) 转换为3次，
  $$
  时间从 (O(n^2)) 降至 (O(n^{\log 3}) \approx O(n^{1.59}))。
  $$
  

- **Strassen矩阵乘法**：
  $$
  将 (n \times n) 矩阵乘法从8次子矩阵乘法减少到7次，时间复杂度 (O(n^{\log_2 7}) \approx O(n^{2.807}))，\\优于传统 (O(n^3))。
  $$
  

##### 2. **预处理减少递归内计算量**

将耗时操作提前至预处理阶段，降低递归中的计算复杂度。

- 平面点对问题

  ：

  - **预处理**：先对点按横纵坐标排序，递归划分后仅需检查跨边界的常数个邻近点（最多6个），
    $$
    总时间 (O(n \log n))。
    $$

  - **关键优化**：
    $$
    利用分治和排序，将暴力法的 (O(n^2)) 优化为线性对数时间。
    $$
    

#### 四、典型实例分析

##### 1. **快速排序（Quicksort）**

- **核心思想**：选择基准元素，将数组划分为小于和大于基准的两部分，递归排序。

- 时间复杂度

  ：

  - $$
    最坏情况（基准每次选最值）：(W(n) = W(n-1) + n-1 = \Theta(n^2))。
    $$

    

  - $$
    平均情况（基准随机选择）：(T(n) = \Theta(n \log n))，通过递推方程和期望分析证明。
    $$

    

##### 2. **选择问题（Selection Problem）**

- 选第k小元素

  ：

  - **锦标赛算法**：
    $$
    通过分组比较找最大/第二大元素，时间 (O(n + \lceil \log n \rceil))。
    $$
    

  - **分治选择算法**：
    $$
    将数组分5个一组找中位数，递归淘汰无关元素，最坏时间 (O(n))（利用主定理分析）。
    $$
    

##### 3. **多项式求值（FFT思想）**

- **问题**：计算多项式在所有 (2n) 次方根处的值。

- **分治方法**：
  $$
  将多项式拆分为偶次项和奇次项，利用根的对称性递归计算，时间复杂度 (O(n \log n))，\\优于暴力法的 (O(n^2))。
  $$
  

#### 五、分治策略关键要素

1. **子问题独立性**：子问题需与原问题类型相同且相互独立（如归并排序的左右子数组）。
2. **均匀划分**：尽量平衡子问题规模（如归并排序均分数组，避免快速排序的极端划分）。
3. **合并效率**：合并子问题解的时间不能抵消分解带来的优化（如归并过程为线性时间）。

分治策略通过减少问题规模和递归结构，显著提升了排序、查找、矩阵运算等问题的效率，是算法设计中的核心技术之一。实际应用中，需结合数学变换和预处理进一步优化，以达到更低的时间复杂度。



